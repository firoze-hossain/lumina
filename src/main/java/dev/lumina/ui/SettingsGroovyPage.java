package dev.lumina.ui;

/**
 * Backward-compatible subclass of SettingsColorSchemeGroovyPage.
 * Dynamically managed via EditorColorSchemeSettings and ColorSettingsPage descriptors.
 */
public class SettingsGroovyPage extends SettingsColorSchemeGroovyPage {
    public SettingsGroovyPage() {
        super();
    }
}